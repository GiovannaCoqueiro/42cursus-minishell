-- exec
    - abrir todos os filhos em looping de uma vez e fazer o processo pai esperar por eles acabarem
    - arrumar leaks quando não encontramos o comando

-- signals

-- redirects
    - se houver varios redirects de entrada, consideramos apenas o ultimo, mas se algum anterior der erro, retornamos erro
    - se houver varios redirects de saida, criamos todos mas consideramos apenas o ultimo

-- exit code

-- terminar builtins
