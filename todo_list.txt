-- expansão de variavel
    - procurar por '$'
    - procurar na lista de variáveis pela variável passada
    - substituir a $key pelo valor
    - ignorar a expansão caso o '$' esteja entre aspas simples 
echo USER $USER "$USER" "$USER $USER" "$USER " '$USER' "'$USER'" $$ $USER'' $AAA "$AAA'ASSAA" $"USER" $MFLAGS AAAA"$MFLAGS"AA

-- exec

-- exit code

-- terminar builtins

